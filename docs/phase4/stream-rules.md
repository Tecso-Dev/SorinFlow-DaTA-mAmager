# Rules for every CRM stream (read fully before starting)

You build part of Phase 4 step 3: the old panel's CRM section rebuilt in the new Next.js panel (frontend-next/). Four streams run in parallel in separate git worktrees; the coordinator merges them. The product is Persian (RTL); UI text Persian, code comments English.

## Setup
1. You are in your own git worktree. First make sure it is on the right base:
   `git log --oneline -1` must show `acc8114` or a descendant. If not: `git fetch origin claude/phase-4-nextjs-frontend-ca3150-p74q6m && git checkout -B <your-branch> origin/claude/phase-4-nextjs-frontend-ca3150-p74q6m`.
   Work on a branch named as given in your task. Commit there. NEVER push, never touch main, never deploy.
2. git identity (repo rule): `git config user.name "sobhan azimzadeh"; git config user.email "sobhan.gjhav.azimzadeh@gmail.com"`. Commit messages: start with feat:/fix:/test:, say what was wrong/missing, what changed, what was checked. NO Co-Authored-By, NO Claude-Session line, no mention of Claude or AI anywhere.
3. Read: CLAUDE.md, docs/FRONTEND.md (the rules every page follows — RTL logical utilities, responsive at 390px with no sideways page scroll, strict CSP: no inline <script>/<style>, no dangerouslySetInnerHTML, no CDN; motion + a 3D touch per section; brand from settings; accessibility), frontend-next/AGENTS.md (Next.js 16 has breaking changes; docs in node_modules/next/dist/docs/).
4. Use the shared pieces, do NOT rebuild them: src/components/panel/kit.tsx (PageHeader, IsoBadge, Section, Toolbar, ToneBadge, LabelBadge, Empty, ErrorNote, ListSkeleton, Pagination, Field, NativeSelect, RingDialog = the OTP dialog look for every form/dialog, useConfirm), src/components/panel/date-input.tsx (JalaliDateInput, MonthGrid), src/lib/jalali.ts, src/lib/crm.ts (labels, price(), fullDeposit(), qs(), exportHref()), src/lib/api.ts (api<T>(path,{json,method}); 403 phone_unverified is handled for you), src/lib/session.ts (useSession, can(user,{perm,roles})), src/lib/format.ts (faNum, faPercent, toman, faDate, parseDigits), src/components/toaster.tsx (toast.success/error/info), shadcn ui in src/components/ui/ (Table, Button, Input, Textarea, Checkbox, Switch, Badge, DropdownMenu, Popover, Sheet, Tabs, Tooltip, ...), src/components/viz.tsx (Reveal, CountUp, Tilt, ...). Look at src/components/dashboard/dashboard.tsx for the house style.
   Data: TanStack Query (useQuery/useMutation, invalidate on success). Never window.prompt/confirm/alert. Numbers/phones typed in dir="ltr" inputs; show digits with faNum. Dates shown Jalali (faDate or formatJalali), entered with JalaliDateInput.
5. Do NOT edit shared files (kit.tsx, date-input.tsx, lib/*, app-shell.tsx, nav.ts, tabs.ts, crm-frame.tsx, globals.css, package.json/lock, other streams' folders). If you truly need a shared change, make the smallest additive change and list it clearly in your final report. Put your components in src/components/crm/<tab>/ and replace only your tabs' src/app/panel/(app)/crm/<tab>/page.tsx (keep them thin: render your client component).
   No new npm dependencies.
6. Backend: do not change it unless your task says so. The CRM inventory (what the old panel does, endpoints, fields): /tmp/claude-0/-home-user-SorinFlow-DaTA-mAmager/740dc0c0-b7ba-50b8-b932-70535f17a9b1/scratchpad/crm/inventory.md — every feature listed for your tabs must exist in the new panel (it is the migration checklist). When in doubt, read the old code (frontend/js/app.js, frontend/index.html) and the route (app/api/routes/crm.py, filing.py).
7. Secrets: none in code or chat. Never open ~/SorinFlow-backups.

## Running it
- A backend with rich sample data is ALREADY running at http://127.0.0.1:8020 (Postgres sorinflow_local, users owner/root/manager1/agent1/agent2, password local-pass-1234 — local throwaway). Do not restart it (others use it). Writing test data through the API is fine.
- In your worktree: `cd frontend-next && npm ci --no-audit --no-fund && npx next typegen`, then run your own dev server: `BACKEND_INTERNAL_URL=http://127.0.0.1:8020 npx next dev -p <your port>` in the background (setsid nohup ... &). Port given in your task.
- Screenshots / checks with the preinstalled browser: node + Playwright at /opt/node22/lib/node_modules/playwright/index.mjs, Chromium at /opt/pw-browsers/chromium-1194/chrome-linux/chrome. Log in with `page.request.post('http://127.0.0.1:<port>/api/session/login', {data:{username:'owner', password:'local-pass-1234'}})` then goto the page. Check desktop 1440x900 dark AND iPhone 13 light: no console errors (ignore "Refused to apply inline style" which only next dev produces), no failed /api calls, no horizontal page scroll (document.documentElement.scrollWidth - innerWidth <= 0). LOOK at the screenshots (Read the png) and fix what looks wrong.
- Before each commit: `cd frontend-next && npx tsc --noEmit && npm run lint` clean.
- E2E: add tests/e2e/next/crm-<your stream>.spec.js (use ./helpers: signIn, watchProblems, noHorizontalScroll, scrollThrough, a11y). Cover each tab: it loads with data, the main create/edit/delete flows through the UI, filters, and a11y(page) returns [] (serious/critical axe). Run them against your dev server:
  `cd <worktree> && npm ci --prefix tests/e2e --no-audit --no-fund` (once), then
  `E2E_DEV=1 E2E_NEXT_URL=http://127.0.0.1:<port> PW_CHROMIUM=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npx --prefix tests/e2e playwright test --config tests/e2e/playwright.next.config.js crm-<stream> --project desktop --project android --project iphone`
  All must pass. Tests must clean up or tolerate data other streams create (use unique names like `e2e-<stream>-${Date.now()}`).

## Finish
Commit (several small commits are welcome) on your branch. Final report (English is fine): branch name + commit list, what each tab does now vs the inventory (every inventory item: done / not done and why), shared-file changes if any, backend changes if any (with tests), test results, and screenshot paths of each tab (desktop dark + iPhone light) saved under /tmp/claude-0/-home-user-SorinFlow-DaTA-mAmager/740dc0c0-b7ba-50b8-b932-70535f17a9b1/scratchpad/crm/shots/<stream>-<tab>-<device>.png.
