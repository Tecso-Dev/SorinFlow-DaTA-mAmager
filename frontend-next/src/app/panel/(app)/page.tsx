import { Dashboard } from "@/components/dashboard/dashboard";

export const metadata = { title: "داشبورد" };

// ponytail: sample data until step 2 wires /api/stats and /api/crm.
export default function DashboardPage() {
  return <Dashboard />;
}
